[project]
name = "govsentry"
version = "0.1.0"
description = "Autonomous malicious DAO governance proposal interceptor on GenLayer GenVM."
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Handik4", email = "ehemati08@gmail.com" }]
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = [
    "genlayer-test>=0.30.0rc2",
    "genlayer-py>=0.19.0rc2",
    "genvm-linter>=0.11.1rc2",
    "pytest>=8",
]

[tool.pytest.ini_options]
testpaths = ["tests/direct"]
pythonpath = ["tests/direct"]
